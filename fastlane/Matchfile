git_url("https://github.com/datanova9999/certificates.git")
skip_confirmation(true)
clone_branch_directly(true)

storage_mode("git")

type("appstore")

app_identifier(["com.FastlaneGeneric.dni"])

username("appmanager@datanova.no")